class DialogScene extends Phaser.Scene {
    constructor() {
        super({ key: 'DialogScene' });
    }

    // Esta scene está reservada para futuras expansiones
    // Por ahora, el sistema de diálogo está implementado como
    // DialogSystem dentro de GameScene

    create() {
        console.log('DialogScene: No se usa en Sprint 1');
    }
}
